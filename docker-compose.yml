services:
  server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENABLE_WEB_INTERFACE=true
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 3s
      start_period: 5s
      retries: 3
